version: 0.2

phases:
  install:
    commands:
      - echo Installing build environment...

  build:
    commands:
      - echo Build started on `date`
      - echo No compilation needed for static website

  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - index.html
    - style.css
    - script.js
